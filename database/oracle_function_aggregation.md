# 함수(집계)

```sql
-- COUNT : 데이터 개수 세기
SELECT COUNT(*)
FROM EMP;

-- SUM : 합계
SELECT SUM(SAL)
FROM EMP;

-- AVG : 평균
SELECT AVG(SAL)
FROM EMP;

-- MIN : 최소값
SELECT MIN(SAL)
FROM EMP;

-- MAX : 최대값
SELECT MAX(SAL)
FROM EMP;
```